name: Build and Deploy Code

on: [push, pull_request]

jobs:
  job1:
    runs_on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v2
      - name: say hi to lakshay
        run: echo "Hello World"